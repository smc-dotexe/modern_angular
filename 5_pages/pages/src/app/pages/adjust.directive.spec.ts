import { AdjustDirective } from './adjust.directive';

describe('AdjustDirective', () => {
  it('should create an instance', () => {
    const directive = new AdjustDirective();
    expect(directive).toBeTruthy();
  });
});
