import { IsActiveClassDirective } from './is-active-class.directive';

describe('IsActiveClassDirective', () => {
  it('should create an instance', () => {
    const directive = new IsActiveClassDirective();
    expect(directive).toBeTruthy();
  });
});
